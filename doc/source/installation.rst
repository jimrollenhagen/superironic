============
Installation
============

At the command line::

    $ pip install superironic

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv superironic
    $ pip install superironic
